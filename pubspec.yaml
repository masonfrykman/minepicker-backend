name: backend
description: A server app using the shelf package and Docker.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  args: ^2.7.0
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  uuid: ^4.5.3
  sqlite3: ^3.2.0
  http: ^1.6.0
  io: ^1.0.5
  dbcrypt: ^2.0.0

dev_dependencies:
  lints: ^6.1.0